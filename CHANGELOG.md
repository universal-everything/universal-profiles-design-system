# Changelog

All notable changes to this repository. The format follows Keep a Changelog; versions follow `GOVERNANCE.md`.

## Unreleased

### Changed

- Asset library: the twelve slide backgrounds under `assets/generated/backgrounds/slides-v2/` are replaced in place by branded versions that show one or more official UP! boxes (the cube mark with the badge face), produced at the product owner's request of 2026-09-15 as image edits of the former generic files guided by the owner's official 3D UP! box reference (Universal Profile Board node `1131:25142`, a private input cited by node id and hash, not published). Every entry now records the generation id, the former file's hash and time stamp as the edit target, the reference citation, the embedded-mark count (by visual review) and the safe zone with its measured figures; the six safe zones are unchanged and re-measured on the new pixels. The two background contact sheets are recomposed from the new files by the new `scripts/compose-previews.mjs` (8-bit RGB instead of the earlier 16-bit tool output); the app showcases, app screens, onboarding art, heroes and title pair are untouched. The file count is unchanged (decision 0012, reconciliation R-39).
- Licensing and trademark texts: `LICENSES/GENERATED-IMAGES.md`, `LICENSES/README.md`, `TRADEMARKS.md` and the provenance records now separate the Apache-2.0 grant on the generated scene pixels from the UP! mark the twelve backgrounds depict, which remains its owner's trademark and is not licensed for extraction, cropping out, tracing or standalone reuse; the contact sheets inherit the same restriction. The earlier statement that the generated images contain no marks is corrected: the hero pair and the title pair show blank objects only, the twelve slide backgrounds show the UP! mark and nothing else that reads as a word or mark.
- Documentation: the README, `assets/README.md`, the backgrounds and slides READMEs, the logos note, the imagery guide and brief IB-12, `brand/lockups.md`, `brand/misuse.md`, the marketing pattern and examples, the agent playbooks, `provenance/README.md`, `provenance/sources.json` (node `1131:25142` on the board source), `CLAUDE.md` and `AGENTS.md` describe the branded set, the owner-authorized depiction (a publication and use decision for these files, not brand approval) and the extraction restriction; a further branded file needs its own recorded authorization.

### Added

- Decision 0012 (`decisions/0012-branded-slide-backgrounds.md`): the official UP! box depicted inside the twelve generated slide backgrounds under the owner's recorded authorization; the standalone reference stays unpublished; extraction or reuse of the embedded mark is not granted; decision 0010 stays in force elsewhere.
- Validation: a `branded` check (`scripts/validate/branded.mjs`, pinned selection in `scripts/validate/branded-backgrounds.json`, unit tests in `scripts/validate/branded.test.mjs`) that compares the twelve files, their prompts (by hash), generation ids, reference citation, safe zones and recorded measurements, the slide-contract thresholds, the two contact sheets (recomposed pixel for pixel, pinned grid and hashes, app-showcase hashes pinned as unchanged), the decision record and its index, the source register, the safe-zone table in the backgrounds README and the trademark and licence wording of twenty-one documents with the pinned values, and rejects any line in those documents that claims the branded backgrounds carry no words, letters, marks or text.
- `scripts/compose-previews.mjs` composes the background contact sheets from the placement table on Node alone (`--check` compares the committed sheets pixel for pixel; write mode keeps a committed sheet whose pixels already match, so `npm run build` leaves the committed bytes and hashes alone on every supported Node); `scripts/lib/png.mjs` gains `scaleArea`, `zoneLuminance` and an 8-bit RGB `encodeRgbPng` writer whose pixels are deterministic while its bytes depend on the zlib the running Node bundles; all covered by tests.
- Asset library: twelve generated slide backgrounds in six families (identity orbits, glass profile stack, address ribbons, modular constellation, identity network, iridescent horizon), each light and dark, under `assets/generated/backgrounds/slides-v2/` with verbatim prompts (also kept in the staged `PROMPTS.source.json`), hashes, content-credentials time stamps and measured text-safe zones (brief IB-12, status proposed; the generic first set, since replaced by the branded set above); five transparent design-file exports of app screens under `assets/screenshots/mobile-app/` (observed; the paywall and deployment screens exploratory); the four shipped onboarding illustrations under `assets/slides/onboarding/` (observed, byte-identical to the mobile bundle at commit `8d8f9f8c`); four previews under `assets/slides/previews/` composed only from repository files. The app screens and onboarding art are named, owner-authorized exceptions to the publication boundary (decision 0011, reconciliation R-37 and R-38, OPEN-04 narrowed).
- Documentation: a visual gallery in the README; `assets/README.md` as the library index; READMEs for the slide and background library (safe zones, light and dark guidance, crops, usage, authority, boundary, adding variations), the app screens and the slides directory; deck section, product slide and background-variation guidance in the marketing pattern, the marketing examples and two new agent playbooks.
- Validation: provenance methods `design-file export`, `shipped-asset copy` and `composition` with per-method required fields, an authorization and a status for observed material, a byte-for-byte cross-check between a staged prompt file and the provenance entries, and a `rasters` check that decodes PNGs on Node alone (`scripts/lib/png.mjs`) to prove the slide contract (16:9, minimum size, 8-bit RGB without alpha, intact C2PA manifest with the recorded software agent and time stamp, a light and a dark file per family, safe-zone luminance and quietness), a real alpha channel with a transparent exterior and a tight crop for the app screens, transparent backgrounds for the onboarding art, and a pixel match between every preview and its declared inputs. `scripts/inspect-png.mjs` prints the facts a provenance entry needs; `scripts/lib/png.test.mjs` covers the reader (every filter type, Adam7, all colour types, 16-bit, palette transparency, alpha facts, corrupt files, manifest summary) and runs inside the gate.

### Fixed

- React Native theme: `darkTheme` now carries dark-resolved `components` and `identicon` objects (`darkComponents`, `darkIdenticon`); previously it reused the light objects, so labels, usernames and button fills rendered light ink on dark surfaces. The validator reads the emitted file back and fails if any component-tier dark override is missing.
- Token metadata: a token's status may no longer outrank the weakest status in its alias chain (decision 0009). Fifty-one tokens that resolved through normalized or proposed values now declare that status with a note; theme-file metadata wins over base metadata, so every dark override is reported as proposed with the dark evidence as its source, and `tokens.dark.flat.json` reports component tokens with the weakest status along their dark chain.
- Icons: the 32-unit drawing grid is documented as a normalization, not an observed product rule; the shipped sets are drawn mostly on 24-unit boxes (R-35). No filled variants are claimed; the `globe` icon is labelled "Web" so it no longer collides with `network`.
- Validator: the generated TypeScript is now really parsed (types stripped with `node:module`, then parsed as ES modules); `node --check` had accepted any ES-module `.ts` file unread. The documented Node floor is 22.13 and CI runs 22.13, 22 and 24.
- Validator: every file must be a known text type or a PNG under `assets/`; session logs, environment files, documents, archives, databases and binaries elsewhere are rejected, binary content in text types is rejected, `.gitignore` must ignore `*.jsonl`, `*.pdf`, `.env` and `.env.*`, and eleven credential patterns (API keys, GitHub, AWS, Slack, Figma and Google tokens, private keys, JWTs, npm auth tokens, environment-style assignments, SSH keys) were added.
- Address Signature: `alpha` reads 0 to 1 as a fraction (1 is opaque) and 2 to 255 as an integer, so 1 no longer means 1/255; generator options (widths, heights, scale, angle, radius, ids, background colours, slice counts) are validated instead of interpolated; `sliceAddress` with a zero trailing count no longer re-appends the whole address; keccak input types are checked. Existing valid outputs are unchanged. Tests cover multi-block absorb, escaping, option validation, whitespace-only names and non-table identicon sizes.
- Provenance: the generated images record the `gpt-image 2.0` software agent, the embedded C2PA content credentials and watermark, a licence and a role label instead of a personal name; provenance records must carry a licence and may never mark a file as reconstructed (decision 0010).
- Documentation: DTCG format deviations, the typography longhands for tracked and uppercase roles, the maintainer wording, node-id citations, generic phrasing for confidential sources, the proposed status of the badge threshold, and the whitespace-trimming normalization of names.
- Validator: files git tracks are validated even when `.gitignore` matches them, so a forced `git add -f` of a session log, a PDF or an environment file no longer passes the gate; only untracked ignored local state is skipped. A `mutation` check plants such files in a throwaway repository, force-adds them and requires the forbidden check to reject exactly those while the untracked ignored copies beside them stay excluded. The gate needs git on the path.
- Documentation: the typography longhand guidance now names only longhands that exist per role. Every role emits a letter-spacing longhand, which the tracked roles (`type.label.caps`, `type.label.nav`, `type.deck.h1`, `type.deck.h2`, `type.deck.h3`) need; only `type.label.caps`, `type.currency.code` and `type.deck.caption` emit a text-transform longhand; the Tailwind preset carries tracking in its `fontSize` entries and no text-transform.
- Icons: `chevron-left` is labelled "Previous" so that it no longer shares "Back" with `arrow-left`, the icon the shipped headers use for back; every default accessible label must now be unique. No SVG changed.
- Provenance: the mobile externally-owned-account row's truncation (`0x` plus 4 characters, dots, 4 characters, then the `#EOA` label) is recorded as an observed variant (R-36); the normalized presets 6 and 4 and 10 and 8 are unchanged.
- Tokens: every record in `tokens.flat.json` and `tokens.dark.flat.json` carries `statusVia`, the token whose weaker status a dark record inherited through its alias chain (null when the status is the token's own, and throughout the light file). No value changed.
- Build: `npm run build` (`build:previews`) no longer rewrites the two background contact sheets on every run. Their bytes depend on the zlib of the running Node (Node 24 and Node 26 encode the same pixels differently), so the former unconditional rewrite on another Node line changed both files and broke the pinned hashes although no pixel had changed. The composer now decodes each committed sheet and writes only when a pixel differs, the size differs or the file cannot be read, keeping the committed bytes and hash otherwise; `scripts/compose-previews.test.mjs` covers the missing, matching, differently encoded, drifted and unreadable cases. The gallery contract was and is the decoded pixels; the wording that called the encoder deterministic now says deterministic pixels.
- Validator: the rasters check, the branded lock and the contact-sheet composer share one box filter (`scaleArea`, with an explicit `enlarge` option for scaling a showcase base up to the preview size) and one safe-zone measurement (`zoneLuminance`) in `scripts/lib/png.mjs` instead of private copies; the outputs are unchanged. The branded tests now cover a missing background or contact sheet and a measurement that is not a number.
- Documentation: brief IB-12 describes the generation constraints as present in substance in every prompt, in the prompts' own words and with exclusion lists that vary by family, instead of "verbatim in every prompt"; decision 0010's status line and index row point to the one owner-authorized depiction recorded in 0012 and state that 0010 remains the default; the logos note no longer describes a `reconstructed: true` provenance entry, which decision 0010 forbids.

## 0.1.0 (2026-09-14)

Initial public structure.

### Added

- DTCG token sources for primitives, semantics, components and the dark theme, with status, source, confidence and open-item metadata on every token; generated CSS, TypeScript, React Native, flat and nested JSON, Tailwind overlay preset and design-tool payloads.
- The UP accent family anchored on the brand board's labelled periwinkle swatch (hue 229, saturation 78), with the board values recorded as observed tokens and roles kept as proposals (OPEN-01).
- Deck typography roles (proposed, OPEN-13).
- `packages/address-signature`: checksum, gradient stops and recipes, suffix, display name, truncation, identicon reimplementation verified against the products' library, aura and share-card SVG generators, with tests and fixtures.
- Procedural address-gradient examples and recipes with provenance.
- Foundations, brand, fifteen component specifications, twelve patterns including the obsolete register, icon rules with a manifest and 53 original starter icons, imagery guide with eleven briefs and safe zones, accessibility rules and checklist, adoption guides, migration checklist, gap register, provenance registers, reconciliation register, eight decision records.
- Four original generated images (hero light and dark, title backgrounds light and dark) with verified provenance.
- Web and React Native examples, marketing recipes, agent playbooks.
- Validation gate (`npm test`): JSON, tokens, build drift, contrast, documents and links, assets and provenance, forbidden content, sources and open items, icons, examples, package hygiene, package tests.
- Governance, contributing, security, code of conduct, licence, trademark and third-party notices.
