# Changelog

All notable changes to this repository. The format follows Keep a Changelog; versions follow `GOVERNANCE.md`.

## Unreleased

### Changed

- Asset library: added the twelve-file `assets/generated/backgrounds/slides-v3-ambient/` family as proposed, additive presentation infrastructure without replacing or weakening the dense `slides-v2` set. Its six light/dark pairs show twenty edge-biased solid, closed and sealed UP! container cubes in total, one or two per scene, with no people, faces, footwear, rockets, third-party marks or uncontrolled text. Exact prompts, generation ids, hashes, C2PA time stamps, references, cube counts and manual visual-review facts are recorded and pinned; the original PNG bytes and content credentials remain intact. Pair-shared safe zones measured from decoded pixels pass the stricter IB-13 contract of light mean at least 220, dark mean at most 36 and deviation at most 12: quiet-corner `0, 0, 0.68, 1` (245.3/6.2 light, 13.4/11.0 dark); quiet-corner-left `0.32, 0, 0.68, 1` (244.4/7.1, 12.4/10.3); distant-horizon `0, 0, 1, 0.72` (242.8/10.6, 17.5/8.6); peripheral-frame `0.2, 0.12, 0.6, 0.76` (251.9/3.0, 21.5/9.8); mist-orbit-right `0, 0, 0.64, 1` (241.9/7.7, 11.6/7.3); mist-orbit-left `0.36, 0, 0.64, 1` (239.9/9.0, 12.7/5.0). Two deterministic ambient overview sheets are added; one unpublished superseded mist-orbit-right dark draft is retained by hash only in the dark mist-orbit lineage. The generated collection now contains 28 images; 24 are marked slide backgrounds, with four background-only overview sheets and six previews in total (decision 0014, reconciliation R-43).
- Documentation, provenance, licensing and policy: added separate light/ink-copy and dark/white-copy usage guidance for the ambient family; recorded the owner's 2026-09-16 authorization for exactly its twelve scenes and two overview sheets without publishing private correspondence; and separated the Apache-2.0 grant on scene pixels from the UP! mark, which remains unlicensed for extraction or standalone reuse. The ambient family remains proposed imagery, its one unpublished draft remains hash-only, and a further branded file still requires its own authorization. The existing slides-v2 authorization, prompts, fixture, files and dense IB-12 role are unchanged.
- Asset library: the twelve slide backgrounds under `assets/generated/backgrounds/slides-v2/` are replaced in place again, this time by the owner-selected closed-cube versions: layered identity collages in the composition language of the shipped onboarding art, each showing one or more official UP! container cubes that are solid, closed and sealed (a monolithic rounded die with softly rounded edges, smooth continuous top and side planes, the white UP! once on the front face and no other readable text; no hole, slot, lid, rim, cavity or insert, nothing entering or emerging) instead of the flat rounded badge the previous set rendered, because the Universal Profile is a container whose contents are shown around the cube. A container-cube draft staged on 2026-09-15 as precise-object edits of the flat-badge files was rejected by the owner for its open-receptacle cubes and never published; its generation ids and hashes are not recorded. Each published file is a fresh generation from its prompt with two or three of the repository's already-public onboarding illustrations (`assets/slides/onboarding/get-started-2.png`, `get-started-3.png`, `get-started-4.png`) as style and composition references, recorded by hash on every entry; no earlier published background and no private export was an input. Fresh hashes, dimensions (all twelve 1672 by 941), C2PA time stamps (2026-09-16T06:43:40Z to 06:58:43Z for the generations; 08:09:42Z and 08:10:52Z for the two sneaker edits below), generation ids, cube counts by visual review (four, four, five, five, five, four, five, five, three, three, three, three, light then dark per family) and freshly measured safe zones (one rectangle per family, shared by the light and dark file: identity orbits 0, 0, 0.28, 0.75; glass profile stack 0.66, 0, 0.34, 1; address ribbons 0.15, 0.05, 0.35, 0.25; modular constellation 0.4, 0.2, 0.22, 0.55; identity network 0, 0, 0.3, 1; iridescent horizon 0, 0, 0.5, 0.42) are recorded; the two contact sheets are recomposed by `scripts/compose-previews.mjs`; the app showcases, app screens, onboarding art, heroes and title pair are untouched (decision 0013, reconciliation R-40).
- Final-review corrections to the closed-cube set (2026-09-16, decision 0013, reconciliation R-41): the light address-ribbons file shows five UP! container cubes, not four, and every record, pin and document now says so; `glass-profile-stack-light.png` and `address-ribbons-dark.png` are replaced by precise-object edits of their own generation output that remove a swoosh-like side stripe from the sneaker in each scene (edit ids `exec-89119ef5-fd56-4cce-a7a0-c753c00fe313` and `exec-a3aeecd9-5fd2-4f88-940a-2bbba5d2674d`, the superseded outputs recorded as edit targets by hash only, the verbatim edit prompts on the entries and in the staged prompt file, the safe zones re-measured on the edited pixels in the same rectangles, both contact sheets recomposed); the provenance schema and `scripts/validate/branded-backgrounds.json` gain the edit block and the edit-prompt contract, and `scripts/validate/branded.mjs` pins each edit's id, prompt and target and rejects an unpinned edit. The polarity-aware geometry guard is widened to the affirmative receptacle wording the review probed (lidded, openable, slotted, hinged, hollow, sunken, concave and indented tops; cards, documents or objects that emerge from, spill out of, slide into or sit inside a cube) and a request that precedes a negation in the same clause is no longer dropped; the documents now describe that guard as a keyword and pattern gate over the prompt text, not a proof of the pixels. The claim that all twelve prompts reject neon is withdrawn: only the three dark prompts of identity orbits, glass profile stack and address ribbons say "not neon cyberpunk" and the dark iridescent-horizon prompt "not cyberpunk". The rule against dreamscapes is stated against generic AI dreamscapes and the parent brand's pink dreamscape covers, with the iridescent-horizon family (its light prompt: "Dreamlike but premium pale landscape") named as the admitted landscape of the set; no pinned prompt is rewritten.
- Final visual cleanup of the closed-cube set (2026-09-16, decision 0013, reconciliation R-42): four files are replaced by owner-authorized precise-object edits of their own previous output so that the last visually ambiguous details are gone rather than qualified. `glass-profile-stack-light.png` (edit `exec-23ec22a4-a981-40cf-8e9e-4b157684c014`) and `address-ribbons-dark.png` (edit `exec-330a18dd-e4a5-4924-a76d-393697f30eba`) no longer contain a sneaker: the shoes of the earlier sneaker edits are replaced by brand-neutral pearlescent spheres and, in the light glass-profile-stack file, a plain frosted-glass medal, and the blank periwinkle cube of the dark address-ribbons file by a sphere, so that file shows four UP! container cubes and no other cube form and the temporary note about a faint hooked seam is withdrawn with the shoes; `identity-orbits-dark.png` (edit `exec-8b54b8f5-1796-43d8-8027-6fb0ca1afdff`) has every cube top re-rendered as a smooth continuous convex glossy plane without the darker inset that could be misread at thumbnail size; `identity-network-light.png` (edit `exec-6497707f-a512-4177-8d2c-e67863711d47`) loses the cone-shaped sculpture behind its central cube, with visible background between the cube top and every object. Fresh hashes, C2PA time stamps (08:42:59Z, 08:43:59Z, 08:44:53Z and 08:45:51Z), the verbatim edit prompts with their kinds, the superseded outputs as edit targets by hash (the two earlier sneaker edits kept as superseded lineage), safe zones re-measured on the final pixels in the same rectangles and both recomposed contact sheets are recorded, mirrored in the staged prompt file and pinned; the edit-prompt contract in `scripts/validate/branded-backgrounds.json` becomes a core contract plus three kinds (object replacement, top-plane cleanup, occlusion removal), `scripts/validate/branded.mjs` checks the chains end at the generation output, requires decision 0013 to name every pinned edit and rejects an unpinned edit or lineage entry, and its affirmative-text heuristic treats a removal clause (remove, delete, erase, eliminate) like a negation. Cube counts are unchanged.
- Documentation and rules: the depicted object is described everywhere as the solid, closed and sealed UP! container cube and never as a flat badge, a tile or anything with openings, lids or inserts; `brand/lockups.md` states the closed geometry as the description of the mark, `brand/misuse.md` forbids the flat-badge and open forms, brief IB-12 requires the closed geometry of every prompt in the positive and the openings rejected in its Avoid line (checked by a keyword and pattern gate over the prompt text), brief IB-06 keeps the error cube closed, and the README, `assets/README.md`, the backgrounds, slides and logos READMEs, the imagery guide, the licence and trademark texts, the marketing pattern and examples, the agent playbooks, `provenance/README.md`, `provenance/sources.json` (both board nodes `1131:25142` and `1737:1014` marked superseded; the onboarding-art source cites the reference use), the provenance schema, `CLAUDE.md`, `AGENTS.md`, `CONTRIBUTING.md` and `SECURITY.md` cite decision 0013, the public references and the re-measured safe zones. The authorization scope, the Apache-2.0 grant on the scene pixels and the extraction restriction on the mark are unchanged.
- Asset library: the twelve slide backgrounds under `assets/generated/backgrounds/slides-v2/` are replaced in place by branded versions that show one or more official UP! boxes, produced at the product owner's request of 2026-09-15 as image edits of the former generic files guided by the owner's reference at the time (Universal Profile Board node `1131:25142`, a private input cited by node id and hash, not published; since superseded, because it is a front-facing badge render and the objects it produced read as flat badges: see the container-cube entry above). Every entry now records the generation id, the former file's hash and time stamp as the edit target, the reference citation, the embedded-mark count (by visual review) and the safe zone with its measured figures; the six safe zones are unchanged and re-measured on the new pixels. The two background contact sheets are recomposed from the new files by the new `scripts/compose-previews.mjs` (8-bit RGB instead of the earlier 16-bit tool output); the app showcases, app screens, onboarding art, heroes and title pair are untouched. The file count is unchanged (decision 0012, reconciliation R-39).
- Licensing and trademark texts: `LICENSES/GENERATED-IMAGES.md`, `LICENSES/README.md`, `TRADEMARKS.md` and the provenance records now separate the Apache-2.0 grant on the generated scene pixels from the UP! mark the twelve backgrounds depict, which remains its owner's trademark and is not licensed for extraction, cropping out, tracing or standalone reuse; the contact sheets inherit the same restriction. The earlier statement that the generated images contain no marks is corrected: the hero pair and the title pair show blank objects only, the twelve slide backgrounds show the UP! mark and nothing else that reads as a word or mark.
- Documentation: the README, `assets/README.md`, the backgrounds and slides READMEs, the logos note, the imagery guide and brief IB-12, `brand/lockups.md`, `brand/misuse.md`, the marketing pattern and examples, the agent playbooks, `provenance/README.md`, `provenance/sources.json` (node `1131:25142` on the board source, since superseded), `CLAUDE.md` and `AGENTS.md` describe the branded set, the owner-authorized depiction (a publication and use decision for these files, not brand approval) and the extraction restriction; a further branded file needs its own recorded authorization.

### Added

- Decision 0014 (`decisions/0014-ambient-slide-backgrounds.md`) and brief IB-13: a separate lower-contrast family for content, chart and app-screen slides; six measured light/dark pairs with one or two cubes, large quiet areas and strict geometry and composition constraints; the owner's date-only authorization for the exact twelve scenes and two sheets; the unpublished draft's hash-only lineage; the proposed status, rights boundary and requirement for a new authorization before any further branded file.
- Validation: a separate `ambient` check with its own pinned fixture and unit tests covers the twelve PNGs, verbatim staged prompts, generation ids, hashes, intact C2PA facts, 1672 by 941 dimensions, light/dark pairs, references, twenty visually reviewed cubes, safe-zone rectangles and measurements, manual composition facts, two deterministically composed overview sheets and required decision and documentation language. It rejects unpinned files or edits, more than two cubes, insufficient quiet area, safe-zone drift, subjects entering the safe zone, third-party marks, people, faces, footwear, rockets, dense or hero-art requests, openings, inserts, flat badges and mark-extraction language. The closed-cube polarity guards are reused without changing the slides-v2 fixture or `branded` check.
- Decision 0013 (`decisions/0013-container-cube-slide-backgrounds.md`): the owner's correction from flat-badge imagery, and from a rejected open-receptacle draft that was never published, to the solid closed UP! container cube inside the layered composition language of the shipped onboarding collages; the closed-cube geometry contract; the already-public onboarding illustrations as the recorded references (by hash) and both board nodes (`1131:25142`, `1737:1014`) superseded; the authorization scope, rights and language clauses of decision 0012 continued; decision 0012's Status line and index row point to it.
- Validation: the `branded` check now carries the closed-cube geometry contract, polarity-aware (`scripts/validate/branded-backgrounds.json`, `geometry`): eleven requirements that every pinned prompt must match in the record and in the staged file whatever its hash (a solid sealed cube, closed, monolithic, a smooth unbroken or uninterrupted top, rounded, the white UP! on the front face, the UP! cube named, no other readable text, opaque, a layered or dense composition, an Avoid line), five requirements on the prompt's Avoid line (holes, openings, slots, lids, rims and cavities rejected; documents, cards or objects entering or emerging from a cube rejected; handles, tabs and protrusions rejected; transparency and watermarks rejected) and four negation guards (the closed geometry stated in the negative anywhere; openings, lids, slots, holes, cavities, rims or receptacles, an object entering or emerging from a cube or a cube that opens, or a flat badge, tile or slab requested in an affirmative clause, where the affirmative text is the prompt without its Avoid lines and without any clause carrying a negation word, so an Avoid clause never fails merely for naming a forbidden artifact); the rules must stay distinct and the tests pin every rule literally. The check also pins the three reference illustrations by hash (on disk, in their record, in the set block, in every entry's generation inputs and in the staged file, with no other input allowed), reads every branded file's header (dimensions, opacity, bit depth, colour type), requires one shared safe-zone rectangle per family that meets the slide contract, requires the galleries to show every file and both sheets, requires the onboarding-art source to cite the decision, and rejects the superseded board exports as inputs, citations or unqualified mentions and the flat-badge and open-receptacle forms in every listed document and record. The tests cover every requirement by removing it from a re-pinned prompt, the removal of the closed and sealed language, positive open-top language in several forms, objects entering or emerging, the closed geometry negated, Avoid clauses and negated clauses that name the artifacts without failing, header drift, reference drift, the superseded nodes, every document phrase, the open-receptacle wording and the decision chain.
- Decision 0012 (`decisions/0012-branded-slide-backgrounds.md`): the official UP! box depicted inside the twelve generated slide backgrounds under the owner's recorded authorization; the standalone reference stays unpublished; extraction or reuse of the embedded mark is not granted; decision 0010 stays in force elsewhere.
- Validation: a `branded` check (`scripts/validate/branded.mjs`, pinned selection in `scripts/validate/branded-backgrounds.json`, unit tests in `scripts/validate/branded.test.mjs`) that compares the twelve files, their prompts (by hash), generation ids, reference citation, safe zones and recorded measurements, the slide-contract thresholds, the two contact sheets (recomposed pixel for pixel, pinned grid and hashes, app-showcase hashes pinned as unchanged), the decision record and its index, the source register, the safe-zone table in the backgrounds README and the trademark and licence wording of twenty-one documents with the pinned values, and rejects any line in those documents that claims the branded backgrounds carry no words, letters, marks or text.
- `scripts/compose-previews.mjs` composes the background contact sheets from the placement table on Node alone (`--check` compares the committed sheets pixel for pixel; write mode keeps a committed sheet whose pixels already match, so `npm run build` leaves the committed bytes and hashes alone on every supported Node); `scripts/lib/png.mjs` gains `scaleArea`, `zoneLuminance` and an 8-bit RGB `encodeRgbPng` writer whose pixels are deterministic while its bytes depend on the zlib the running Node bundles; all covered by tests.
- Asset library: twelve generated slide backgrounds in six families (identity orbits, glass profile stack, address ribbons, modular constellation, identity network, iridescent horizon), each light and dark, under `assets/generated/backgrounds/slides-v2/` with verbatim prompts (also kept in the staged `PROMPTS.source.json`), hashes, content-credentials time stamps and measured text-safe zones (brief IB-12, status proposed; the generic first set, since replaced by the branded set above); five transparent design-file exports of app screens under `assets/screenshots/mobile-app/` (observed; the paywall and deployment screens exploratory); the four shipped onboarding illustrations under `assets/slides/onboarding/` (observed, byte-identical to the mobile bundle at commit `8d8f9f8c`); four previews under `assets/slides/previews/` composed only from repository files. The app screens and onboarding art are named, owner-authorized exceptions to the publication boundary (decision 0011, reconciliation R-37 and R-38, OPEN-04 narrowed).
- Documentation: a visual gallery in the README; `assets/README.md` as the library index; READMEs for the slide and background library (safe zones, light and dark guidance, crops, usage, authority, boundary, adding variations), the app screens and the slides directory; deck section, product slide and background-variation guidance in the marketing pattern, the marketing examples and two new agent playbooks.
- Validation: provenance methods `design-file export`, `shipped-asset copy` and `composition` with per-method required fields, an authorization and a status for observed material, a byte-for-byte cross-check between a staged prompt file and the provenance entries, and a `rasters` check that decodes PNGs on Node alone (`scripts/lib/png.mjs`) to prove the slide contract (16:9, minimum size, 8-bit RGB without alpha, intact C2PA manifest with the recorded software agent and time stamp, a light and a dark file per family, safe-zone luminance and quietness), a real alpha channel with a transparent exterior and a tight crop for the app screens, transparent backgrounds for the onboarding art, and a pixel match between every preview and its declared inputs. `scripts/inspect-png.mjs` prints the facts a provenance entry needs; `scripts/lib/png.test.mjs` covers the reader (every filter type, Adam7, all colour types, 16-bit, palette transparency, alpha facts, corrupt files, manifest summary) and runs inside the gate.

### Fixed

- React Native theme: `darkTheme` now carries dark-resolved `components` and `identicon` objects (`darkComponents`, `darkIdenticon`); previously it reused the light objects, so labels, usernames and button fills rendered light ink on dark surfaces. The validator reads the emitted file back and fails if any component-tier dark override is missing.
- Token metadata: a token's status may no longer outrank the weakest status in its alias chain (decision 0009). Fifty-one tokens that resolved through normalized or proposed values now declare that status with a note; theme-file metadata wins over base metadata, so every dark override is reported as proposed with the dark evidence as its source, and `tokens.dark.flat.json` reports component tokens with the weakest status along their dark chain.
- Icons: the 32-unit drawing grid is documented as a normalization, not an observed product rule; the shipped sets are drawn mostly on 24-unit boxes (R-35). No filled variants are claimed; the `globe` icon is labelled "Web" so it no longer collides with `network`.
- Validator: the generated TypeScript is now really parsed (types stripped with `node:module`, then parsed as ES modules); `node --check` had accepted any ES-module `.ts` file unread. The documented Node floor is 22.13 and CI runs 22.13, 22 and 24.
- Validator: every file must be a known text type or a PNG under `assets/`; session logs, environment files, documents, archives, databases and binaries elsewhere are rejected, binary content in text types is rejected, `.gitignore` must ignore `*.jsonl`, `*.pdf`, `.env` and `.env.*`, and eleven credential patterns (API keys, GitHub, AWS, Slack, Figma and Google tokens, private keys, JWTs, npm auth tokens, environment-style assignments, SSH keys) were added.
- Address Signature: `alpha` reads 0 to 1 as a fraction (1 is opaque) and 2 to 255 as an integer, so 1 no longer means 1/255; generator options (widths, heights, scale, angle, radius, ids, background colours, slice counts) are validated instead of interpolated; `sliceAddress` with a zero trailing count no longer re-appends the whole address; keccak input types are checked. Existing valid outputs are unchanged. Tests cover multi-block absorb, escaping, option validation, whitespace-only names and non-table identicon sizes.
- Provenance: the generated images record the `gpt-image 2.0` software agent, the embedded C2PA content credentials and watermark, a licence and a role label instead of a personal name; provenance records must carry a licence and may never mark a file as reconstructed (decision 0010).
- Documentation: DTCG format deviations, the typography longhands for tracked and uppercase roles, the maintainer wording, node-id citations, generic phrasing for confidential sources, the proposed status of the badge threshold, and the whitespace-trimming normalization of names.
- Validator: files git tracks are validated even when `.gitignore` matches them, so a forced `git add -f` of a session log, a PDF or an environment file no longer passes the gate; only untracked ignored local state is skipped. A `mutation` check plants such files in a throwaway repository, force-adds them and requires the forbidden check to reject exactly those while the untracked ignored copies beside them stay excluded. The gate needs git on the path.
- Documentation: the typography longhand guidance now names only longhands that exist per role. Every role emits a letter-spacing longhand, which the tracked roles (`type.label.caps`, `type.label.nav`, `type.deck.h1`, `type.deck.h2`, `type.deck.h3`) need; only `type.label.caps`, `type.currency.code` and `type.deck.caption` emit a text-transform longhand; the Tailwind preset carries tracking in its `fontSize` entries and no text-transform.
- Icons: `chevron-left` is labelled "Previous" so that it no longer shares "Back" with `arrow-left`, the icon the shipped headers use for back; every default accessible label must now be unique. No SVG changed.
- Provenance: the mobile externally-owned-account row's truncation (`0x` plus 4 characters, dots, 4 characters, then the `#EOA` label) is recorded as an observed variant (R-36); the normalized presets 6 and 4 and 10 and 8 are unchanged.
- Tokens: every record in `tokens.flat.json` and `tokens.dark.flat.json` carries `statusVia`, the token whose weaker status a dark record inherited through its alias chain (null when the status is the token's own, and throughout the light file). No value changed.
- Build: `npm run build` (`build:previews`) no longer rewrites the two background contact sheets on every run. Their bytes depend on the zlib of the running Node (Node 24 and Node 26 encode the same pixels differently), so the former unconditional rewrite on another Node line changed both files and broke the pinned hashes although no pixel had changed. The composer now decodes each committed sheet and writes only when a pixel differs, the size differs or the file cannot be read, keeping the committed bytes and hash otherwise; `scripts/compose-previews.test.mjs` covers the missing, matching, differently encoded, drifted and unreadable cases. The gallery contract was and is the decoded pixels; the wording that called the encoder deterministic now says deterministic pixels.
- Validator: the rasters check, the branded lock and the contact-sheet composer share one box filter (`scaleArea`, with an explicit `enlarge` option for scaling a showcase base up to the preview size) and one safe-zone measurement (`zoneLuminance`) in `scripts/lib/png.mjs` instead of private copies; the outputs are unchanged. The branded tests now cover a missing background or contact sheet and a measurement that is not a number.
- Documentation: brief IB-12 describes the generation constraints as present in substance in every prompt, in the prompts' own words and with exclusion lists that vary by family, instead of "verbatim in every prompt"; decision 0010's status line and index row point to the one owner-authorized depiction recorded in 0012 and state that 0010 remains the default; the logos note no longer describes a `reconstructed: true` provenance entry, which decision 0010 forbids.

## 0.1.0 (2026-09-14)

Initial public structure.

### Added

- DTCG token sources for primitives, semantics, components and the dark theme, with status, source, confidence and open-item metadata on every token; generated CSS, TypeScript, React Native, flat and nested JSON, Tailwind overlay preset and design-tool payloads.
- The UP accent family anchored on the brand board's labelled periwinkle swatch (hue 229, saturation 78), with the board values recorded as observed tokens and roles kept as proposals (OPEN-01).
- Deck typography roles (proposed, OPEN-13).
- `packages/address-signature`: checksum, gradient stops and recipes, suffix, display name, truncation, identicon reimplementation verified against the products' library, aura and share-card SVG generators, with tests and fixtures.
- Procedural address-gradient examples and recipes with provenance.
- Foundations, brand, fifteen component specifications, twelve patterns including the obsolete register, icon rules with a manifest and 53 original starter icons, imagery guide with eleven briefs and safe zones, accessibility rules and checklist, adoption guides, migration checklist, gap register, provenance registers, reconciliation register, eight decision records.
- Four original generated images (hero light and dark, title backgrounds light and dark) with verified provenance.
- Web and React Native examples, marketing recipes, agent playbooks.
- Validation gate (`npm test`): JSON, tokens, build drift, contrast, documents and links, assets and provenance, forbidden content, sources and open items, icons, examples, package hygiene, package tests.
- Governance, contributing, security, code of conduct, licence, trademark and third-party notices.
