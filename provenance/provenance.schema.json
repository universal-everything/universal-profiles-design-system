{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "provenance.schema.json",
  "title": "Asset provenance record",
  "description": "One PROVENANCE.json per asset directory (a record may cover sub-directories). Every file in the directory must be listed. Validated by scripts/validate.mjs (hash, dimensions, required fields per method, licence, status vocabulary) and, when the record carries a contract, measured pixel by pixel by the rasters check.",
  "type": "object",
  "required": ["method", "license", "assets"],
  "properties": {
    "method": { "type": "string", "enum": ["ai-generated", "procedural", "vector", "3d-render", "photo", "OpenAI built-in image generation", "design-file export", "shipped-asset copy", "composition"], "description": "How the files came to be. Generated methods need a prompt per raster; design-file export needs a source key, a node id per file, a status and an authorization; shipped-asset copy needs a source key, the repository and commit, a source path per file, a status and an authorization; composition needs the inputs and placements per file" },
    "source": { "type": "string", "pattern": "^SRC-[A-Z0-9-]+$", "description": "Source register key the files come from (design-file export, shipped-asset copy)" },
    "sourceFile": { "type": "object", "description": "Design file cited by title and file key only; never a URL" },
    "sourceRepository": { "type": "object", "description": "Repository, commit, version and path the files were copied from" },
    "authorization": { "type": "string", "description": "Who admitted observed material past the publication boundary, when, and the decision record that logs it" },
    "status": { "type": "string", "enum": ["observed", "normalized", "proposed", "obsolete", "open"], "description": "Status of the record's files in the repository vocabulary" },
    "exportedAt": { "type": "string" },
    "copiedAt": { "type": "string" },
    "composedAt": { "type": "string" },
    "sets": { "type": "object", "description": "Named groups of assets in the record (directory, brief, count, families, generation date); entries cite a set by name and the validator checks the counts" },
    "contract": { "type": "object", "description": "Pixel-level requirements the rasters check measures: aspectRatio and aspectTolerance, minWidth, minHeight, dimensions, bitDepth, colourType, alphaChannel, cornersTransparent, tight, maxTransparentFraction, contentCredentials, pairs, lightRegister.minSafeZoneLuminance, darkRegister.maxSafeZoneLuminance, maxSafeZoneDeviation, maxMeanAbsoluteDifference" },
    "tool": { "type": "string" },
    "model": { "type": "string", "description": "Model or software agent as exposed by the tool or by credentials embedded in the files" },
    "contentCredentials": { "type": "object", "description": "Provenance embedded in the files themselves (for example a C2PA manifest): standard, software agent, claim generator, actions and signer as read from the file" },
    "operator": { "type": "string", "description": "A role label (for example 'design-system maintainer'), never a personal name" },
    "author": { "type": "string" },
    "generatedAt": { "type": "string" },
    "seed": { "type": ["integer", "string", "null"] },
    "license": { "type": "string", "description": "Licence statement for the files in the directory; required and checked by the validator" },
    "rightsNote": { "type": "string" },
    "publication": { "type": "string", "enum": ["public", "internal"] },
    "postProcessing": { "type": "string", "description": "Edits after generation, or a statement that there were none, including delivered versus requested dimensions" },
    "assets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "sha256"],
        "properties": {
          "path": { "type": "string", "description": "Relative to the directory of the PROVENANCE.json" },
          "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "dimensions": { "type": "string", "pattern": "^[0-9]+x[0-9]+$", "description": "Required for raster files; checked against the file header" },
          "brief": { "type": "string", "description": "Brief identifier from imagery/briefs.md, for example IB-02" },
          "prompt": { "type": "string", "description": "Verbatim prompt for generated images" },
          "method": { "type": "string" },
          "inputs": { "type": "object" },
          "postProcessing": { "type": "string" },
          "reconstructed": { "type": "boolean", "const": false, "description": "Reserved. The repository ships no reconstruction of a mark under any label (decisions 0007 and 0010); the validator rejects an entry that sets this to true" },
          "status": { "type": "string", "enum": ["observed", "normalized", "proposed", "obsolete", "open"] },
          "set": { "type": "string", "description": "Name of the set in the record's sets block" },
          "family": { "type": "string", "description": "Family name shared by a light and a dark file" },
          "register": { "type": "string", "enum": ["light", "dark"] },
          "safeZone": { "type": "object", "description": "Text-safe zone as fractions x, y, width, height of the image, with a note; measured by the validator against the contract", "properties": { "x": { "type": "number" }, "y": { "type": "number" }, "width": { "type": "number" }, "height": { "type": "number" }, "note": { "type": "string" } } },
          "colour": { "type": "string", "description": "Bit depth, colour type and alpha facts as read from the file" },
          "contentCredentials": { "type": "object", "description": "Per-file facts read from the embedded manifest, for example the time-stamp authority time" },
          "node": { "type": "string", "pattern": "^[0-9]+:[0-9]+$", "description": "Design-file node the file was exported from" },
          "title": { "type": "string" },
          "classification": { "type": "string", "description": "For design-file exports: current design-file screen or exploratory design-file screen" },
          "description": { "type": "string" },
          "sourcePath": { "type": "string", "description": "Path inside the source repository for shipped-asset copies" },
          "composition": { "type": "object", "description": "Inputs of a composed preview: an optional base (path, fit) and placements (path, x, y, width, height), all repository assets with their own provenance", "properties": { "canvas": { "type": "string" }, "base": { "type": "object" }, "placements": { "type": "array", "items": { "type": "object", "required": ["path", "x", "y", "width", "height"] } } } }
        }
      }
    }
  }
}
